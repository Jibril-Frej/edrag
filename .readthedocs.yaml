# .readthedocs.yaml

version: 2

# Specify the Read the Docs build image version
build:
  os: ubuntu-22.04
  tools:
    python: "3.12"

# Build documentation in the "docs" directory
sphinx:
  configuration: docs/conf.py
